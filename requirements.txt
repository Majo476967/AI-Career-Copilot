# Existing runtime dependencies, verified against the local Python 3.10 environment.
langchain==1.3.18
langchain-core==1.6.1
langchain-openai==1.6.0
openai==3.7.0
python-dotenv==1.2.3
# Phase 1 storage and tests use stdlib sqlite3 / unittest; no ORM or test package needed.
pypdf==6.18.0
python-docx==1.2.0

# Phase 5 local product UI (Python 3.10 compatible).
streamlit==1.63.0
